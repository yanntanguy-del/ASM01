global _start

_start:
    ; "o world" → 8 octets
    mov rax, 0x646c726f77206f
    push rax

    ; "hell" → 4 octets
    push dword 0x6c6c6568

    mov rax, 1
    mov rdi, 1
    mov rsi, rsp
    mov rdx, 11
    syscall

    mov rax, 60
    xor rdi, rdi
    syscall
