global _start

_start:
    mov rax, 0x646c726f77206f
    push rax

    push rax

    mov rax, 1
    mov rdi, 1
    mov rsi, rsp
    mov rdx, 11
    syscall

    mov rax, 60
    xor rdi, rdi
    syscall
